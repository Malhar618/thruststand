%% JSON Converter for Fellowship support 
% The purpose of this function is to convert the gains from Giorgio's 
% MATLAB code into a JSON format for usage on Mattia's flightstack.


% @author Colleen Piccolo
% @version March 14, 2025
function  json = JSON_converter_final(gainsMatrix)
    KPt = 10;
    KDt = 11;
    KIt = 12;
    KPr = 13;
    KDr = 14;
    KIr = 15;
    % Kp_eta = diag(gainsMatrix(1:3));
    % Ki_eta = diag(gainsMatrix(4:6));
    % Kd_eta = diag(gainsMatrix(7:9));
    file = struct('KP_translational', ...
        struct('scaling_coeff', 1.0, 'matrix', KPt), ...
        'KD_translational', ...
        struct('scaling_coeff', 1.0, 'matrix', KDt), ...
        'KI_translational', ...
        struct('scaling_coeff', 1.0, 'matrix', KIt), ...
        'KP_rotational', ...
        struct('scaling_coeff', 1.0, 'matrix', [14.0, 0.0, 0.0; ...
        0.0, 14.0, 0.0; ...
        0.0, 0.0, 25.0]), ...
        'KD_rotational', ...
        struct('scaling_coeff', 1.0, 'matrix', [14.0, 0.0, 0.0; ...
        0.0, 14.0, 0.0; ...
        0.0, 0.0, 25.0]), ...
        'KI_rotational', struct('scaling_coeff', 1.0, 'matrix', [14.0, 0.0, 0.0; ...
        0.0, 14.0, 0.0; ...
        0.0, 0.0, 25.0]))
    json = jsonencode(file)
    % format the json appropriately to match the formatting of mattia's
    % json 
    
end